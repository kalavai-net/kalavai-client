services:
  kalavai_gui:
    container_name: kalavai_gui
    image: bundenth/kalavai-gui:latest
    ports:
      - "{{gui_port}}:3000"
      - "{{backend_port}}:8000"
    volumes:
      - "{{path}}:/root/.cache/kalavai"
    restart: always
