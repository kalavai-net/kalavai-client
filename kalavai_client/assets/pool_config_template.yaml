# # # storage class for model weights (temporary)
# kind: StorageClass
# apiVersion: storage.k8s.io/v1
# metadata:
#   name: {{storage_class_name}}
# provisioner: driver.longhorn.io
# allowVolumeExpansion: true
# reclaimPolicy: Delete
# volumeBindingMode: Immediate
# parameters:
#   shareManagerNodeSelector: {{storage_label_selector}}
#   numberOfReplicas: "{{storage_replicas}}"
#   staleReplicaTimeout: "10"
#   fromBackup: ""
#   fsType: "ext4"
#   dataLocality: "disabled"
#   unmapMarkSnapChainRemoved: "ignored"
#   disableRevisionCounter: "true"
#   dataEngine: "v1"

# Langfuse secrets
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: langfuse
  namespace: kalavai
stringData:
  salt: {{langfuse_salt_key}}
  encryption-key: {{langfuse_encryption_key}} # Generate with `openssl rand -hex 32`
  nextauth-secret: {{langfuse_nextauth_secret}}
  redis-password: {{langfuse_redis_password}}
  # s3-user: {{langfuse_s3_user}}
  # s3-password: {{langfuse_s3_password}}
